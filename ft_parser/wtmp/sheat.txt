$a$b$c\$l

$"abc"

$"abc$db"

"$a$b"$c\$l
"
$a
$b"
$c\$l


is double quoted
	split until the end of the double quote by $
else
	split normaly


--------------------------------------------------------------------------------
if $ is followed by single quote
	convert espace sequences accordinally, the rest remains unmodified.
if $ is followed by double quote
	ignore the $ and expand the rest.

to handle $'' contruct :
	$ has to be unquoted
