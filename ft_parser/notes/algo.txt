quotes
-------------------------------------------------------------------
esc == '
	append
esc == "
	tmp == \ or $ or `
		\ only if followed by \ or $ or ` or " or newline
esp	== \
	append
-------------------------------------------------------------------

history
-------------------------------------------------------------------
the shell loads the history from the history file and writes to it
only at exit.
all changes duration execution are kept on the program until the 
shell exits.
-------------------------------------------------------------------

operators
-------------------------------------------------------------------
op == && or ||
	exit status of the last command executed according to the logical operator
op == |
	exit status of the last command; FIFO
op == ;
	exit status of the last command; end of command sequences
-------------------------------------------------------------------


-------------------------------------------------------------------
echo abc | cat | grep b && ls; pwd
-------------------------------------------------------------------

-------------------------------------------------------------------
echo abc 
|
cat 
|
 grep b
&&
 ls
;
 pwd
-------------------------------------------------------------------

-------------------------------------------------------------------
-------------- pipeline --------------
echo abc 
|
cat 
|
 grep b
-------------- operator --------------
&&
-------------- pipeline --------------
 ls
-------------- operator --------------
;
-------------- pipeline --------------
 pwd
-------------------------------------------------------------------



-	expand every word except RDC_OPERANDS
-	wherever va is set don't expand
-	
